
import { Box, Typography, useTheme, IconButton } from "@mui/material";

export default Letter = ({ letterPos, round }) => {

    


    return (
        <Box
            className="letter"
            id={letterState}
        >
            {letter}
        </Box>
    )
}